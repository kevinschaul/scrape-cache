{
  "name": "scrape-cache",
  "version": "0.0.1",
  "description": "Scrape web pages, storing pages locally to lessen repetitive network requests.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Schaul",
  "license": "MIT",
  "devDependencies": {
    "nodeunit": "^0.9.1"
  },
  "dependencies": {
    "cheerio": "^0.19.0",
    "request": "^2.66.0",
    "slug": "^0.9.1"
  }
}
